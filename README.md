## Laravel Vue basic crud

run SQL and upload the sql `vuecrud.sql`

set `.env` database to `DB_DATABASE=vuecrud`

run these commands

```
composer install

npm install

npm run watch

php artisan serve
```
